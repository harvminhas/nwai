/**
 * Operator emails allowed to call `/api/admin/*` routes.
 * Keep in sync with client-side checks (e.g. {@link isDebugSuperAdmin}).
 */
export const ADMIN_EMAILS = ["harvminhas@gmail.com"] as const;
